# NeatChests weights configuration.
# Items and categories with higher weights are sorted first.
#
# Categories determine the primary sorting order.
# Items override their category weight when present.

